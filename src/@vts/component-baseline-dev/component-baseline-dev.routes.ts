import { Routes } from '@angular/router';

import {
  ComponentsBaselinePageComponent
} from './containers';

export const ROUTES: Routes = [
  { path: 'component-baseline-dev', component: ComponentsBaselinePageComponent } ];
